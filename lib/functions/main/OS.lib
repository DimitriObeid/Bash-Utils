#!/usr/bin/env bash

# ---------------------
# SCRIPT'S INFORMATIONS

# Name          : OS.lib
# Module        : Main
# Description   : Operating systems types and versions management.
# Author(s)     : Dimitri Obeid
# Version       : beta

# ----------------------------
# SHELLCHECK GLOBAL DISABLER :

# Add a coma after each warning code to disable multiple warnings at one go.

# Do not uncomment the "shellcheck disable" line, or else the shellcheck command will be executed during the script's execution, and will not detect any coding mistake during a debugging process.

# DO NOT PUT A COMA AFTER A SHELLCHECK CODE IF THERE'S NO OTHER SHELLCHECK CODE FOLLOWING IT, OR ELSE SHELLCHECK WILL RETURN ERRORS DURING THE DEBUGGING PROCESS !!!

# IF YOU WANT TO ADD ANOTHER SHELLCHECK CODE, WRITE THIS CODE DIRECTLY AFTER THE COMMA, WITHOUT ADDING A BLANK SPACE AFTER IT !!!

# shellcheck disable=SC2154

# --------------------------------------------------------------------------------------------------------------------------------------------
# DO NOT EXECUTE THIS SCRIPT DIRECTLY, instead, just source it by calling the "$__BU_MAIN_FUNCTIONS_FILES_PATH" array in the initializer file.

# /////////////////////////////////////////////////////////////////////////////////////////////// #

# Preventing the direct execution of this file, as this script is not meant to be directly executed, but sourced.
if [ "${0##*/}" == "${BASH_SOURCE[0]##*/}" ]; then
    echo -e "WARNING !" >&2; echo >&2;
    echo -e "This shell script (${BASH_SOURCE[0]}) is not meant to be executed directly !" >&2;
    echo -e "Use this script only by sourcing it in your project script." >&2; echo >&2;

    exit 1;
fi

# ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;; #

#################################### DEFINING LIBRARY FUNCTIONS ###################################

#### OPERATING SYSTEM CHECKINGS
#### DEBUG ID : Checkings.operating-system-checkings

__BU_MAIN_OS_LIB__CAT_DEBUG_ID__OPERATING_SYSTEM_CHECKINGS="osoperating-system-checkings";

## SPECIFIC OS VERSIONS CHECKINGS FUNCTIONS
## DEBUG ID : specific-os-versions-checkings
__BU_MAIN_OS_LIB__SUBCAT_DEBUG_ID__OPERATING_SYSTEM_CHECKINGS__SPECIFIC_OS_VERSIONS_CHECKINGS_FUNCTIONS="$__BU_MAIN_OS_LIB__CAT_DEBUG_ID__OPERATING_SYSTEM_Checkings.specific-os-versions-checkings";

# Checking the name of the currently used Linux distribution.
function BU.Main.OS.CheckLinuxDistro()
{
    # This function makes it easier for the user to find the current function and its executed content if it is called during the execution of the main script with the command "bash -x".
    BU.Main.Echo.Debug "$(basename "${BASH_SOURCE[0]}")" "${FUNCNAME[0]}" "$__BU_MAIN_OS_LIB__CAT_DEBUG_ID__OPERATING_SYSTEM_CHECKINGS" "$__BU_MAIN_OS_LIB__SUBCAT_DEBUG_ID__OPERATING_SYSTEM_CHECKINGS__SPECIFIC_OS_VERSIONS_CHECKINGS_FUNCTIONS";

    #**** Parameters ****
    local v_distro=$1;  # String    - Default : NULL    - Name of the Linux distribution.

    #**** Code ****

    BU.Main.OS.IsOSLinux && {
        if [ -n "$v_distro" ]; then                         if BU.Main.OS.GetLinuxDistro "$v_distro"; then return 0; else BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 1; fi

        else
            BU.Main.OS.IsOSLinuxDistroArchLinux             && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
            BU.Main.OS.IsOSLinuxDistroDebian                && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
            BU.Main.OS.IsOSLinuxDistroFedora                && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
            BU.Main.OS.IsOSLinuxDistroLinuxMint             && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
            BU.Main.OS.IsOSLinuxDistroManjaro               && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
            BU.Main.OS.IsOSLinuxDistroUbuntu                && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;

            # Returning an error code if the distribution is not found.
            BU.Main.Echo.DebugEnd "${FUNCNAME[0]}";

            return 1;
        fi
    }
}

# Checking the version of the currently used Linux distribution.
function BU.Main.OS.CheckLinuxDistroVersion()
{
    #**** Parameters ****
	local p_distro=$1;	# String    - Default : NULL    - Name of the Linux distribution.
    local p_version=$2;	# String    # Default : NULL    - Linux distribution's version (can be an integer or a string (like 'Focal Fossa' for Ubuntu 20.04)).

    #**** Code ****
    BU.Main.OS.IsOSLinux && {
        if [ -n "$p_distro" ] && [ -n "$p_version" ]; then
            if BU.Main.OS.GetLinuxDistro "$p_distro"         && BU.Main.OS.GetLinuxDistroVersion "$p_version"; then return 0; else BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 1; fi

        else
            BU.Main.OS.IsOSLinuxDistroFedora && {
                BU.Main.OS.IsOSLinuxDistroFedora28          && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
                BU.Main.OS.IsOSLinuxDistroFedora29          && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
                BU.Main.OS.IsOSLinuxDistroFedora30          && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
                BU.Main.OS.IsOSLinuxDistroFedora31          && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
                BU.Main.OS.IsOSLinuxDistroFedora32          && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
                BU.Main.OS.IsOSLinuxDistroFedora33          && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
                BU.Main.OS.IsOSLinuxDistroFedora34          && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
                BU.Main.OS.IsOSLinuxDistroFedora35          && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
                BU.Main.OS.IsOSLinuxDistroFedora36          && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;

                # Returning an error code if the specific version is not found.
                BU.Main.Echo.DebugEnd "${FUNCNAME[0]}";

                return 1;
            }

            BU.Main.OS.IsOSLinuxDistroUbuntu && {
                BU.Main.OS.IsOSLinuxDistroUbuntu1804        && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
                BU.Main.OS.IsOSLinuxDistroUbuntu1810        && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
                BU.Main.OS.IsOSLinuxDistroUbuntu1904        && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
                BU.Main.OS.IsOSLinuxDistroUbuntu1910        && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
                BU.Main.OS.IsOSLinuxDistroUbuntu2004        && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
                BU.Main.OS.IsOSLinuxDistroUbuntu2010        && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
                BU.Main.OS.IsOSLinuxDistroUbuntu2104        && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
                BU.Main.OS.IsOSLinuxDistroUbuntu2110        && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
                BU.Main.OS.IsOSLinuxDistroUbuntu2204        && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;

                # Returning an error code if the specific version is not found.
                BU.Main.Echo.DebugEnd "${FUNCNAME[0]}";

                return 1;
            }

            # Returning an error code if the distribution is not found.
            BU.Main.Echo.DebugEnd "${FUNCNAME[0]}";

            return 1;
        fi
    }
}

# Getting the name of the currently used Linux distribution, by reading the content of the "/proc/version" file on Linux.
function BU.Main.OS.GetLinuxDistro()
{
    #**** Parameters ****
    local v_distro=${1:-NULL};  # String    # Default : NULL    - Linux distribution's version (can be an integer or a string (like 'Focal Fossa' for Ubuntu 20.04)).

    #**** Code ****
    if BU.Main.OS.IsOSLinux; then [ -r /proc/version ] && cat /proc/version | grep -qi "$v_distro" || return 1; return 0; else return 1; fi
}

# Getting the name of the currently used Linux distribution, by reading the content of the "/proc/version" file on Linux.
function BU.Main.OS.GetLinuxDistroVersion()
{
    BU.Main.OS.GetLinuxDistro "$1" || return 1; return 0;
}

# Checking the current operating system, by using the functions declared above.
function BU.Main.OS.GetCurrentOS()
{
    # This function makes it easier for the user to find the current function and its executed content if it is called during the execution of the Fedoramain script with the command "bash -x".
    BU.Main.Echo.Debug "$(basename "${BASH_SOURCE[0]}")" "${FUNCNAME[0]}" "$__BU_MAIN_OS_LIB__CAT_DEBUG_ID__OPERATING_SYSTEM_CHECKINGS" "$__BU_MAIN_OS_LIB__SUBCAT_DEBUG_ID__OPERATING_SYSTEM_CHECKINGS__SPECIFIC_OS_VERSIONS_CHECKINGS_FUNCTIONS";

    #**** Code ****
    BU.Main.OS.IsOSAndroidTermux        && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
    BU.Main.OS.IsOSFreeBSD              && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
    BU.Main.OS.IsOSHaiku                && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
    BU.Main.OS.IsOSOpenBSD              && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
    BU.Main.OS.IsOSLinux                && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
    BU.Main.OS.IsOSLinuxWSLWin10        && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
    BU.Main.OS.IsOSLinuxWSLWin11        && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
    BU.Main.OS.IsOSLinuxWSL             && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
    BU.Main.OS.IsOSNetBSD               && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
    BU.Main.OS.IsOSWindows10            && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
    BU.Main.OS.IsOSWindows11            && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;
    BU.Main.OS.IsOSX                    && BU.Main.Echo.DebugEnd "${FUNCNAME[0]}"; return 0;

    # Return an error code if the OS is not supported.
    BU.Main.Echo.DebugEnd "${FUNCNAME[0]}";

    return 1;
}

# Checking the Microsoft Windows version.
function BU.Main.Checkings.CheckOSWindowsVersion()
{
    #**** Parameters ****
    local p_versionNumber=${1:-NULL};   # Int       - Default : NULL    - Microsoft Windows operating system's version number.

    #**** Code ****
    if [ -z "$__BU_MAIN_OS_LIB__WINDOWS_MAJOR_VERSION_NUMBER" ]; then __BU_MAIN_OS_LIB__WINDOWS_MAJOR_VERSION_NUMBER="$(powershell.exe '[System.Environment]OSVersion.Version' | awk 'FNR == 4 {print $1}')"; fi

    if [ "$p_versionNumber" -eq "$__BU_MAIN_OS_LIB__WINDOWS_MAJOR_VERSION_NUMBER" ]; then return 0; else return 1; fi

    return 0;
}

# -----------------------------------------------

## GENERAL OS VERSIONS CHECKINGS
## DEBUG ID : general-os-versions-checkings

# shellcheck disable=SC2034
__BU_MAIN_OS_LIB__SUBCAT_DEBUG_ID__OPERATING_SYSTEM_CHECKINGS__GENERAL_OS_VERSIONS_CHECKINGS="$__BU_MAIN_OS_LIB__CAT_DEBUG_ID__OPERATING_SYSTEM_Checkings.general-os-versions-checkings";

# Checking if current OS is Android, running with the Termux terminal emulator.
function BU.Main.OS.IsOSAndroidTermux()             { if [[ "$OSTYPE" == 'linux-android' ]];                            then    return 0; else  return 1; fi }

# Checking if current OS is FreeBSD.
function BU.Main.OS.IsOSFreeBSD()                   { if [[ "$OSTYPE" == 'FreeBSD' ]];                                  then    return 0; else  return 1; fi }

# Checking if current OS is Haiku OS.
function BU.Main.OS.IsOSHaiku()                     { if [[ "$OSTYPE" == 'haiku' ]];                                    then    return 0; else  return 1; fi }

# Checking if current OS is Linux (or Android without the Termux terminal emulator).
function BU.Main.OS.IsOSLinux()                     { if [[ "$OSTYPE" == 'linux'* ]];                                   then    return 0; else  return 1; fi }

# Checking if current OS is a Linux distribution running on Windows Subsystem for Linux, on any Windows version.
function BU.Main.OS.IsOSLinuxWSL()                  { if [[ "$(< /proc/sys/kernel/osrelease)" == *[M-m]icrosoft* ]];    then    return 0; else  return 1; fi }

# Checking if current OS is NetBSD.
function BU.Main.OS.IsOSNetBSD()                    { if [[ "$OSTYPE" == '' ]];                                         then    return 0; else  return 1; fi }

# Checking if current OS is OpenBSD.
function BU.Main.OS.IsOSOpenBSD()                   { if [[ "$OSTYPE" == 'openbsd'* ]];                                 then    return 0; else  return 1; fi }

# Checking if current OS is OSx.
function BU.Main.OS.IsOSX()                         { if [[ "$OSTYPE" == 'darwin'* ]];                                  then    return 0; else  return 1; fi }

# -----------------------------------------------

## SPECIFIC OS VERSIONS CHECKINGS (FREE BSD)
## DEBUG ID : specific-os-versions-checkings-freebsd

__BU_MAIN_OS_LIB__SUBCAT_DEBUG_ID__OPERATING_SYSTEM_CHECKINGS__SPECIFIC_OS_VERSIONS_CHECKINGS_FREEBSD="$__BU_MAIN_OS_LIB__CAT_DEBUG_ID__OPERATING_SYSTEM_CHECKINGS::specific-os-versions-checkings-freebsd";

# -----------------------------------------------

## SPECIFIC OS VERSIONS CHECKINGS (HAIKU)
## DEBUG ID : specific-os-versions-checkings-haiku

__BU_MAIN_OS_LIB__SUBCAT_DEBUG_ID__OPERATING_SYSTEM_CHECKINGS__SPECIFIC_OS_VERSIONS_CHECKINGS_HAIKU="$__BU_MAIN_OS_LIB__CAT_DEBUG_ID__OPERATING_SYSTEM_CHECKINGS::specific-os-versions-checkings-haiku";

# -----------------------------------------------

## SPECIFIC OS VERSIONS CHECKINGS (LINUX)
## DEBUG ID : specific-os-versions-checkings-linux

# shellcheck disable=SC2034
__BU_MAIN_OS_LIB__SUBCAT_DEBUG_ID__OPERATING_SYSTEM_CHECKINGS__SPECIFIC_OS_VERSIONS_CHECKINGS_LINUX="$__BU_MAIN_OS_LIB__CAT_DEBUG_ID__OPERATING_SYSTEM_CHECKINGS::specific-os-versions-checkings-linux";

# Checking if current Linux distribution is Arch Linux.
function BU.Main.OS.IsOSLinuxDistroArchLinux()          { BU.Main.OS.GetLinuxDistro ARCH                            &&      return 0;       return 1; }


# Checking if current Linux distribution is Debian.
function BU.Main.OS.IsOSLinuxDistroDebian()             { BU.Main.OS.GetLinuxDistro debian                          &&      return 0;       return 1; }

# Checking debian versions.
function BU.Main.OS.IsOSLinuxDistroDebian6()            { BU.Main.OS.CheckLinuxDistroVersion 'Debian' '6'           &&      return 0;       return 1; }
function BU.Main.OS.IsOSLinuxDistroDebian7()            { BU.Main.OS.CheckLinuxDistroVersion 'Debian' '7'           &&      return 0;       return 1; }
function BU.Main.OS.IsOSLinuxDistroDebian8()            { BU.Main.OS.CheckLinuxDistroVersion 'Debian' '8'           &&      return 0;       return 1; }
function BU.Main.OS.IsOSLinuxDistroDebian9()            { BU.Main.OS.CheckLinuxDistroVersion 'Debian' '9'           &&      return 0;       return 1; }
function BU.Main.OS.IsOSLinuxDistroDebian10()           { BU.Main.OS.CheckLinuxDistroVersion 'Debian' '10'          &&      return 0;       return 1; }
function BU.Main.OS.IsOSLinuxDistroDebian11()           { BU.Main.OS.CheckLinuxDistroVersion 'Debian' '11'          &&      return 0;       return 1; }


# Checking if current Linux distribution is Fedora.
function BU.Main.OS.IsOSLinuxDistroFedora()             { BU.Main.OS.GetLinuxDistro Fedora                          &&      return 0;       return 1; }

# Checking Fedora versions.
function BU.Main.OS.IsOSLinuxDistroFedora28()           { BU.Main.OS.CheckLinuxDistroVersion 'Fedora' '28'          &&      return 0;       return 1; }
function BU.Main.OS.IsOSLinuxDistroFedora29()           { BU.Main.OS.CheckLinuxDistroVersion 'Fedora' '29'          &&      return 0;       return 1; }
function BU.Main.OS.IsOSLinuxDistroFedora30()           { BU.Main.OS.CheckLinuxDistroVersion 'Fedora' '30'          &&      return 0;       return 1; }
function BU.Main.OS.IsOSLinuxDistroFedora31()           { BU.Main.OS.CheckLinuxDistroVersion 'Fedora' '31'          &&      return 0;       return 1; }
function BU.Main.OS.IsOSLinuxDistroFedora32()           { BU.Main.OS.CheckLinuxDistroVersion 'Fedora' '32'          &&      return 0;       return 1; }
function BU.Main.OS.IsOSLinuxDistroFedora33()           { BU.Main.OS.CheckLinuxDistroVersion 'Fedora' '33'          &&      return 0;       return 1; }
function BU.Main.OS.IsOSLinuxDistroFedora34()           { BU.Main.OS.CheckLinuxDistroVersion 'Fedora' '34'          &&      return 0;       return 1; }
function BU.Main.OS.IsOSLinuxDistroFedora35()           { BU.Main.OS.CheckLinuxDistroVersion 'Fedora' '35'          &&      return 0;       return 1; }
function BU.Main.OS.IsOSLinuxDistroFedora36()           { BU.Main.OS.CheckLinuxDistroVersion 'Fedora' '36'          &&      return 0;       return 1; }


# Checking if current Linux distribution is Manjaro.
function BU.Main.OS.IsOSLinuxDistroManjaro()            { BU.Main.OS.GetLinuxDistro Manjaro                         &&      return 0;       return 1; }


# Checking if current Linux distribution is Linux Mint.
function BU.Main.OS.IsOSLinuxDistroLinuxMint()          { BU.Main.OS.GetLinuxDistro ''                              &&      return 0;       return 1; }


# Checking if current Linux distribution is SUSE.
function BU.Main.OS.IsOSLinuxDistroSUSE()               { BU.Main.OS.GetLinuxDistro ''                              &&      return 0;       return 1; }


# Checking if current Linux distribution is Ubuntu.
function BU.Main.OS.IsOSLinuxDistroUbuntu()             { BU.Main.OS.GetLinuxDistro Ubuntu                          &&      return 0;       return 1; }

# Checking Ubuntu versions.
function BU.Main.OS.IsOSLinuxDistroUbuntu1804()         { BU.Main.OS.CheckLinuxDistroVersion 'Ubuntu' '18.04'       &&      return 0;       return 1; }
function BU.Main.OS.IsOSLinuxDistroUbuntu1810()         { BU.Main.OS.CheckLinuxDistroVersion 'Ubuntu' '18.10'       &&      return 0;       return 1; }
function BU.Main.OS.IsOSLinuxDistroUbuntu1904()         { BU.Main.OS.CheckLinuxDistroVersion 'Ubuntu' '19.04'       &&      return 0;       return 1; }
function BU.Main.OS.IsOSLinuxDistroUbuntu1910()         { BU.Main.OS.CheckLinuxDistroVersion 'Ubuntu' '19.10'       &&      return 0;       return 1; }
function BU.Main.OS.IsOSLinuxDistroUbuntu2004()         { BU.Main.OS.CheckLinuxDistroVersion 'Ubuntu' '20.04'       &&      return 0;       return 1; }
function BU.Main.OS.IsOSLinuxDistroUbuntu2010()         { BU.Main.OS.CheckLinuxDistroVersion 'Ubuntu' '20.10'       &&      return 0;       return 1; }
function BU.Main.OS.IsOSLinuxDistroUbuntu2104()         { BU.Main.OS.CheckLinuxDistroVersion 'Ubuntu' '21.04'       &&      return 0;       return 1; }
function BU.Main.OS.IsOSLinuxDistroUbuntu2110()         { BU.Main.OS.CheckLinuxDistroVersion 'Ubuntu' '21.10'       &&      return 0;       return 1; }
function BU.Main.OS.IsOSLinuxDistroUbuntu2204()         { BU.Main.OS.CheckLinuxDistroVersion 'Ubuntu' '22.04'       &&      return 0;       return 1; }

# -----------------------------------------------

## SPECIFIC OS VERSIONS CHECKINGS (OSX)
## DEBUG ID : specific-os-versions-checkings-osx

# shellcheck disable=SC2034
__BU_MAIN_OS_LIB__SUBCAT_DEBUG_ID__OPERATING_SYSTEM_CHECKINGS__SPECIFIC_OS_VERSIONS_CHECKINGS_OSX="$__BU_MAIN_OS_LIB__CAT_DEBUG_ID__OPERATING_SYSTEM_Checkings.specific-os-versions-checkings-osx";

# Checking OSX versions.
function BU.Main.OS.IsOSXYosemite()                     { true; }
function BU.Main.OS.IsOSXElCapitan()                    { true; }
function BU.Main.OS.IsOSXSierra()                       { true; }
function BU.Main.OS.IsOSXHighSierra()                   { true; }
function BU.Main.OS.IsOSXMojave()                       { true; }
function BU.Main.OS.IsOSXCatalina()                     { true; }

# -----------------------------------------------

## SPECIFIC OS VERSIONS CHECKINGS (WINDOWS)
## DEBUG ID : specific-os-versions-checkings-windows

# shellcheck disable=SC2034
__BU_MAIN_OS_LIB__SUBCAT_DEBUG_ID__OPERATING_SYSTEM_CHECKINGS__SPECIFIC_OS_VERSIONS_CHECKINGS_WINDOWS="$__BU_MAIN_OS_LIB__CAT_DEBUG_ID__OPERATING_SYSTEM_Checkings.specific-os-versions-checkings-windows";

# Checking if the currently used version of Windows is the 10th.
function BU.Main.OS.IsOSWindows10()                     { if BU.Main.Checkings.CheckOSWindowsVersion '10';          then    return 0; else  return 1; fi }

# Checking if the currently used version of Windows is the 11th.
function BU.Main.OS.IsOSWindows11()                     { if BU.Main.Checkings.CheckOSWindowsVersion '11';          then    return 0; else  return 1; fi }

# Checking if current OS is a Linux distribution running on Windows Subsystem for Linux version 1, on any Windows version (WARNING : here, the 'M' from the "microsoft" string MUST be in uppercase, as the 'm' in lower case is form the WSL version 2).
function BU.Main.OS.IsOSLinuxWSL1()                     { uname -r | grep Microsoft                                 &&      return 0;       return 1; }

# Checking if current OS is a Linux distribution running on Windows Subsystem for Linux version 2, on any Windows version (WARNING : here, the 'm' from the "microsoft" string MUST be in lowercase, as the 'M' in upper case is form the WSL version 1).
function BU.Main.OS.IsOSLinuxWSL2()                     { uname -r | grep microsoft                                 &&      return 0;       return 1; }

# -----------------------------------------------



# /////////////////////////////////////////////////////////////////////////////////////////////// #
